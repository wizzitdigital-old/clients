{
  "name": "@wizzit-clients/aken_proxy",
  "version": "0.0.7",
  "description": "Graphql aken proxy client",
  "main": "lib/index",
  "dependencies": {
    "@wizzit-clients/core": "^0.0.7",
    "axios": "^0.19.0",
    "graphql-tag": "^2.10.1"
  },
  "files": [
    "lib"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "generate-meta": "node ./scripts/generate-meta.js",
    "clean": "rm -rf ./lib",
    "compile": "tsc -p ./tsconfig.build.json",
    "build": "npm run clean && npm run compile"
  }
}
